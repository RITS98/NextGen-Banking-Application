services:
  postgres:
    build:
      context: .
      dockerfile: ./backend/docker/local/postgres/Dockerfile
    ports:
      - "5437:5432"
    volumes:
      - nextgen_local_db:/var/lib/postgresql/db
    env_file:
      - ./.envs/.env.local
    networks:
      - nextgen_local_nw

networks:
  nextgen_local_nw:
    external: false

volumes:
  nextgen_local_db: